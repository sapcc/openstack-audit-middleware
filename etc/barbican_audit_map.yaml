service_type: 'data/security'
service_name: 'barbican'

prefix: '/v1'

resources:
  secrets:
    children:
      metadata:
        # collection of fields/keys
        singleton: true
        type_name: meta
      acl:
  secret-stores:
    children:
      preferred:
  containers:
    children:
      secrets:
      acl:
  project-quotas:
  consumers:
  orders:
  